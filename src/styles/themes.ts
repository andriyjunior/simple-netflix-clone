import { colors } from './variables';

export const themes = {
  dark: {
    alpha: colors.alpha,
    beta: colors.beta,
    gamma: colors.gamma,
    delta: colors.delta,
    darkGrey: colors.darkGrey,
    white: colors.white,
    black: colors.black
  },
  light: {
    alpha: colors.beta,
    beta: colors.alpha,
    gamma: colors.gamma,
    delta: colors.delta,
    darkGrey: colors.white,
    white: colors.black,
    black: colors.white
  }
};
